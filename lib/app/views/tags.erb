<html>
  <body>
    <h1>here are your '<%= tag %>' ideas</h1>
    <form action='/' method='POST'>

    <h3>ideas by tags</h3>
    <ul type=none> <% ideas.each do |idea| %>
        <li>
          </form>
          idea title: <%= idea.title %><br/>
          <%= idea.description %><br/>
          <% idea.tags.split(", ").each do |tag|%>
             <a href="/<%= tag %>"><%= tag%></a>
          <% end %>
          <div>
          <a href="/<%= idea.id %>/edit">Edit</a><br/>
          </div>
          <form action='/<%= idea.id %>' method='POST'>
          </form>
        </li>
      <% end %>
    </ul>
  </body>
</html>
